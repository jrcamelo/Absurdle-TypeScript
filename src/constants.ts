export const enum GameState {
    PLAYING = "playing",
    WON = "won",
    LOST = "lost"
};

export const enum LetterState {
    ABSENT = "absent",
    PRESENT = "present",
    CORRECT = "correct",
};

export const DEFAULT_LIVES = 6;